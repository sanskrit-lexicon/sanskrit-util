# Project Objective: canonical Sanskrit string helpers (Python + JS, behaviour-identical) for the CDSL / Sanskrit-Lexicon repos

## ➡️ Next Steps (Queue)

- [x] **Releases now cut through v0.6.0** (re-verified 14-07-2026, H922) — v0.4.0 (04-07-2026, CDSL
      source-line→IAST), v0.5.0 (13-07-2026, epistemic dashboard tooling), v0.6.0 (14-07-2026,
      `csl_pyutil.render_review_sheet`, H925) all have real GitHub Releases. The stray `v0.2.0` tag
      is gone from origin (retargeted/deleted). v0.3.0 was folded into main without its own tag —
      harmless, the CHANGELOG section still documents it.
- [x] **Wave-2 consumer migrations — re-verified 14-07-2026 (H922):**
  - SanskritSpellCheck `slp1util.py` — **settled, partial-by-design** (SLP1 sets + norm delegate to
    the package; confusion model/sort-key/data-loaders are repo-specific with no canonical home —
    not drift). No further migration owed.
  - csl-atlas `dict-normalize.normalizeLemma` — **settled, deliberately local** (CDSL
    SLP1-headword-specific; transcoders already migrated via csl-atlas PR #119).
  - SamudraManthanam sanscript drop — **still open**, `morph_service.py` still imports
    `indic_transliteration.sanscript` directly for all 3 directions (IAST/Deva→SLP1, SLP1→IAST,
    SLP1→Devanāgarī). The ṁ-handling + `slp1_to_devanagari` round-trip blockers are gone (both
    shipped in v0.3.0), but the **real remaining blocker is packaging**: the service is Dockerized
    and its image copies only `web/`, not this sibling repo — a plain relative import will
    `ImportError` in the container. Needs a packaging decision (vendor a copy vs. pip-install from
    GitHub) before the swap, not just an import change.
  - WhitneyRoots browser IIFE — **still open**, no IIFE/global build has been shipped; readers still
    carry inline `linguistics.js`/`reader.js` copies.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (none)

## 🧠 Dev Notes & Hypotheses

- **`slp1_to_devanagari` is a REAL transcode, not a display replace.** It supplies the virāma between clustered consonants and picks independent-vowel vs mātrā by position. Its maps are **inverted from the same Devanāgarī→SLP1 maps `deva_to_slp1` uses** (kept in lock-step), so the pair is a true round-trip: `deva_to_slp1(slp1_to_devanagari(s)) == s` for canonical SLP1. Only the 3 marks are hardcoded (M→anusvāra, H→visarga, ~→candrabindu) because anusvāra and candrabindu both map back to `M` on the Deva→SLP1 side and can't be inverted unambiguously.
- **Two documented not-round-trip-stable cases** (they match `deva_to_slp1`'s own lossy behaviour, so they are excluded from the round-trip fixture): candrabindu (`~`→ँ→`M` anusvāra) and avagraha (`'`→ऽ, which `deva_to_slp1` drops). SLP1 accents (`/ \ ^`) pass through unchanged.
- **`slp1_simplify` was stranded.** It was pushed to `origin/feat/deva-to-slp1` + tagged `v0.2.0` but **never PR'd/merged**, because it was Python-only (no JS port / vectors / docs) — which violates the repo's parity contract. PR #11 recovers it AND completes it to contract.
- **Versions were stale at 0.1.0** in all three of `pyproject.toml` / `package.json` / `__init__.py` even though the CHANGELOG documented an (unreleased-on-`main`) 0.2.0. PR #11 bumps to **0.3.0** consistently and the CHANGELOG's `Unreleased` SLP1 content is promoted into the 0.3.0 section.
- **`ṁ` (U+1E41)** was already handled by `to_slp1` (`→M`); PR #11 just adds golden-vector + unit-test coverage. This is the named blocker for dropping `sanscript` from SamudraManthanam's IAST→SLP1 path.
- Round-trip fixture [`vectors/slp1_roundtrip_sample.txt`](https://github.com/sanskrit-lexicon/sanskrit-util/blob/main/vectors/slp1_roundtrip_sample.txt) = 1000 real MW `<k1>` headwords (canonical-SLP1-only filter), sampled deterministically from csl-orig `mw.txt`; regenerate with the scratch extractor if `mw.txt` changes. `tools/gen_vectors.py` runs the round-trip + the SLP1-set donor lock on every regen (both skip gracefully if the sibling repos are absent).

## ✅ Completed (Recent only)

- **H082 — SLP1-side API (v0.3.0), 03-07-2026 (Opus 4.8 `claude-opus-4-8`).** `slp1_to_devanagari` (Py+JS) + round-trip property test (alphabet + 1000 MW headwords); `slp1_simplify` recovered with JS parity + vectors + docs; SLP1 set constants donor-locked vs SanskritSpellCheck `slp1util.py`; `ṁ`→`M` coverage; README "which key" + table rows; CHANGELOG 0.3.0; version bump. 418→482 golden vectors, JS==Python==global build all green. PR [#11](https://github.com/sanskrit-lexicon/sanskrit-util/pull/11) **MERGED 2026-07-03**. SHARED_CODE §1–2 SLP1-gap note flipped to shipped; roadmap + handoff registry (H082 → 🔵) + GTD updated.

_Dr. Mārcis Gasūns_
